# CopilotKit / AG-UI pre-release packages have optional peer conflicts; do not fail install.
legacy-peer-deps=true

# Fail fast when Node version does not match package.json engines.
engine-strict=true
